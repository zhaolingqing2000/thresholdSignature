[package]
name = "binding-program"
version = "0.1.0"
edition = "2021"

[dependencies]
binding-core = { path = "../binding-core", default-features = false }
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
sp1-zkvm = "=6.1.0"
